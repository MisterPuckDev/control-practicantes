spring:
  application:
    name: control-practicantes

  datasource:
    url: jdbc:postgresql://localhost:5432/control_practicantes
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8080